import { borders } from "./border";
import { colors } from "./color";
import { paddings, radius, spaces } from "./layout";
import { shadows } from "./shadow";
import { fontSizes } from "./text";

export const theme = {
	colors,
	spaces,
	paddings,
	fontSizes,
	radius,
	shadows,
	borders,
};
